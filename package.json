{
  "name": "autocar-visual-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "visual:test": "playwright test -c playwright.config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
