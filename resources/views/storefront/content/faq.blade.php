@extends('layouts.storefront') @section('title','سؤالات متداول | اتوکار') @section('content')<div class="container py-5"><h1 class="h3 mb-4">سؤالات متداول</h1>@foreach($faqs as $group=>$items)<h2 class="h5 mt-4">{{ $group ?: 'عمومی' }}</h2><div class="accordion" id="faq-{{ $loop->index }}">@foreach($items as $faq)<div class="accordion-item"><h3 class="accordion-header"><button class="accordion-button collapsed" data-bs-toggle="collapse" data-bs-target="#faq{{ $faq->id }}">{{ $faq->question }}</button></h3><div id="faq{{ $faq->id }}" class="accordion-collapse collapse"><div class="accordion-body">{{ $faq->answer }}</div></div></div>@endforeach</div>@endforeach</div>@endsection
