@extends('layouts.storefront') @section('title','علاقه‌مندی‌ها | اتوکار') @section('content')<div class="container py-5"><h1 class="h3 mb-4">علاقه‌مندی‌ها</h1><div class="row g-3">@forelse($wishlist->products as $product)<div class="col-md-6 col-lg-3"><div class="ac-surface p-3 h-100"><a href="{{ route('product.show',$product) }}"><b>{{ $product->name }}</b><div class="mt-2">{{ number_format($product->sale_price) }} ریال</div></a><form method="post" action="{{ route('wishlist.destroy',$product) }}" class="mt-3">@csrf @method('delete')<button class="btn btn-sm btn-outline-danger">حذف</button></form></div></div>@empty<p class="text-muted">محصولی ذخیره نکرده‌اید.</p>@endforelse</div></div>@endsection
