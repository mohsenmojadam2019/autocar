<article class="product-card"><a class="product-media" href="{{ route('product.show',$product->slug) }}">@if($product->media->first())<img loading="lazy" src="{{ asset('storage/'.$product->media->first()->path) }}" alt="{{ $product->media->first()->alt ?: $product->name }}">@else<div class="product-placeholder"><i class="bi bi-gear-wide-connected"></i></div>@endif<span class="auth-badge">{{ $product->authenticity?->value ?? 'company' }}</span></a><div class="product-body"><small>{{ $product->brand?->name ?: 'AutoCar' }}</small><h3><a href="{{ route('product.show',$product->slug) }}">{{ $product->name }}</a></h3><div class="product-code">SKU: {{ $product->sku }} @if($product->oem_code) · OEM: {{ $product->oem_code }}@endif</div><div class="product-bottom"><strong>{{ number_format($product->sale_price) }} <small>ریال</small></strong><form method="post" action="{{ route('cart.add',$product) }}">@csrf<button class="round-add" aria-label="افزودن به سبد"><i class="bi bi-plus-lg"></i></button></form></div></div></article>
