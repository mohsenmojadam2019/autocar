@extends('layouts.storefront')
@section('title','امنیت دومرحله‌ای | اتوکار')
@section('content')<div class="container py-5"><div class="ac-surface p-4 mx-auto" style="max-width:700px"><h1>احراز هویت دومرحله‌ای</h1>@if($confirmed)<div class="alert alert-success">2FA فعال است.</div>@else<p>Secret زیر را در Google Authenticator، Microsoft Authenticator یا برنامه سازگار با TOTP وارد کنید و کد ۶ رقمی را تأیید کنید.</p><div class="border rounded p-3 mb-3"><b>Secret:</b> <code style="direction:ltr;display:inline-block">{{ $secret }}</code><br><small style="word-break:break-all">{{ $uri }}</small></div><form method="post" action="{{ route('account.2fa.confirm') }}">@csrf<label>کد ۶ رقمی</label><input class="form-control mb-3" name="code" inputmode="numeric" required><button class="btn btn-primary">فعال‌سازی</button></form>@endif @if($confirmed)<hr><form method="post" action="{{ route('account.2fa.disable') }}">@csrf @method('delete')<label>رمز عبور برای غیرفعال‌سازی</label><input class="form-control mb-2" type="password" name="password" required><button class="btn btn-outline-danger">غیرفعال‌سازی 2FA</button></form>@endif</div></div>@endsection
