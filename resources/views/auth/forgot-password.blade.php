@extends('layouts.storefront') @section('title','بازیابی رمز عبور | اتوکار') @section('content')<div class="container py-5"><div class="row justify-content-center"><div class="col-md-6 col-lg-5"><div class="ac-surface p-4"><h1 class="h4 mb-3">بازیابی رمز عبور</h1><form method="post" action="{{ route('password.otp') }}">@csrf<label>شماره موبایل</label><div class="input-group mb-3"><input class="form-control" name="mobile" value="{{ old('mobile',session('password_reset_mobile')) }}" required><button class="btn btn-outline-primary">ارسال کد</button></div></form>@if(session('otp_requested'))<form method="post" action="{{ route('password.reset') }}">@csrf<input type="hidden" name="mobile" value="{{ session('password_reset_mobile') }}"><label>کد ۶ رقمی</label><input class="form-control mb-2" name="code" required><label>رمز جدید</label><input class="form-control mb-2" type="password" name="password" required><label>تکرار رمز</label><input class="form-control mb-3" type="password" name="password_confirmation" required><button class="btn btn-primary w-100">تغییر رمز</button></form>@if(session('development_otp'))<div class="alert alert-warning mt-3">OTP محیط توسعه: {{ session('development_otp') }}</div>@endif @endif</div></div></div></div>@endsection
