@extends('layouts.storefront')
@section('title','ورود | اتوکار')
@section('content')<div class="auth-wrap"><div class="auth-card ac-surface"><div class="ac-brand text-center mb-4">AUTO<span>CAR</span></div><h1>ورود به حساب</h1><p>با رمز عبور یا کد یکبارمصرف وارد شوید.</p><ul class="nav nav-pills auth-tabs" role="tablist"><li><button class="nav-link active" data-bs-toggle="tab" data-bs-target="#password">رمز عبور</button></li><li><button class="nav-link" data-bs-toggle="tab" data-bs-target="#otp">کد یکبارمصرف</button></li></ul><div class="tab-content pt-3"><div id="password" class="tab-pane fade show active"><form method="post" action="{{ route('login.password') }}">@csrf<label>ایمیل یا موبایل</label><input class="form-control mb-3" name="login" value="{{ old('login') }}" required><label>رمز عبور</label><input class="form-control mb-3" type="password" name="password" required><button class="btn btn-primary w-100">ورود</button></form></div><div id="otp" class="tab-pane fade"><form method="post" action="{{ route('login.otp.request') }}">@csrf<label>شماره موبایل</label><div class="d-flex gap-2"><input class="form-control" name="mobile" placeholder="0912..." required><button class="btn btn-dark">ارسال کد</button></div></form>@if(session('otp_requested'))<form class="mt-3" method="post" action="{{ route('login.otp.verify') }}">@csrf<input type="hidden" name="mobile" value="{{ session('otp_mobile',old('mobile')) }}"><label>کد ۶ رقمی</label><input class="form-control mb-2" name="code" inputmode="numeric" required>@if(session('development_otp'))<small class="text-muted">کد محیط توسعه: {{ session('development_otp') }}</small>@endif<button class="btn btn-primary w-100 mt-2">تأیید و ورود</button></form>@endif</div></div></div></div>@endsection
