@extends('layouts.storefront') @section('title','سفارش '.$order->number.' | اتوکار') @section('content')<div class="container py-5"><div class="d-flex justify-content-between flex-wrap gap-2 mb-4"><div><h1 class="h3">سفارش {{ $order->number }}</h1><span class="badge text-bg-light">{{ $order->status->value }}</span></div><div class="d-flex gap-2"><a class="btn btn-outline-primary" href="{{ route('account.orders.invoice',$order->number) }}" target="_blank">فاکتور PDF</a><a class="btn btn-light" href="{{ route('account.orders') }}">بازگشت</a></div></div><div class="row g-4"><div class="col-lg-8"><section class="ac-surface p-4 mb-4"><h5>اقلام</h5><div class="table-responsive"><table class="table"><thead><tr><th>کالا</th><th>SKU</th><th>تعداد</th><th>مبلغ</th></tr></thead><tbody>@foreach($order->items as $item)<tr><td>{{ $item->name }}</td><td>{{ $item->sku }}</td><td>{{ $item->quantity }}</td><td>{{ number_format($item->line_total) }}</td></tr>@endforeach</tbody></table></div></section><section class="ac-surface p-4"><h5>پیگیری ارسال</h5>@forelse($order->shipments as $shipment)<div class="border rounded p-3 mb-2"><b>{{ $shipment->carrier ?: 'ارسال فروشگاه' }}</b><div>وضعیت: {{ $shipment->status }}</div><div>کد رهگیری: {{ $shipment->tracking_code ?: '—' }}</div></div>@empty<p class="text-muted">هنوز مرسوله ایجاد نشده است.</p>@endforelse</section></div><div class="col-lg-4"><section class="ac-surface p-4 mb-4"><h5>خلاصه پرداخت</h5><div class="d-flex justify-content-between"><span>جمع نهایی</span><b>{{ number_format($order->grand_total) }} ریال</b></div>@foreach($order->payments as $payment)<hr><div><b>{{ $payment->gateway }}</b> — {{ $payment->status }}</div>@if($payment->gateway==='card_to_card' && $payment->status!=='verified')<form class="mt-3" method="post" enctype="multipart/form-data" action="{{ route('account.payments.proof',$payment->id) }}">@csrf<input class="form-control mb-2" name="reference_code" placeholder="شماره پیگیری"><input class="form-control mb-2" name="card_last4" maxlength="4" placeholder="۴ رقم آخر کارت"><input class="form-control mb-2" type="file" name="receipt" accept=".jpg,.jpeg,.png,.pdf"><button class="btn btn-primary w-100">ثبت رسید</button></form>@endif @endforeach</section>@if(in_array($order->status->value,['shipped','delivered'],true))<section class="ac-surface p-4"><h5>درخواست مرجوعی</h5><form method="post" action="{{ route('account.orders.return',$order->number) }}">@csrf @foreach($order->items as $item)<label class="d-flex justify-content-between mb-2"><span>{{ $item->name }}</span><input style="width:80px" class="form-control form-control-sm" type="number" min="0" max="{{ $item->quantity }}" name="items[{{ $item->id }}]" value="0"></label>@endforeach<textarea class="form-control my-2" name="reason" required placeholder="دلیل مرجوعی"></textarea><button class="btn btn-outline-danger w-100">ثبت RMA</button></form></section>@endif</div></div></div>@endsection
