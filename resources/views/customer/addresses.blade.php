@extends('layouts.storefront') @section('title','آدرس‌ها | اتوکار') @section('content')<div class="container py-5"><div class="row g-4"><div class="col-lg-7"><h1 class="h3 mb-4">آدرس‌های من</h1>@forelse($addresses as $address)<div class="ac-surface p-4 mb-3"><div class="d-flex justify-content-between"><b>{{ $address->title ?: $address->full_name }}</b>@if($address->is_default)<span class="badge text-bg-success">پیش‌فرض</span>@endif</div><p class="mb-1">{{ $address->province }}، {{ $address->city }} — {{ $address->address }}</p><small>{{ $address->mobile }} | {{ $address->postal_code }}</small><form class="mt-2" method="post" action="{{ route('account.addresses.destroy',$address) }}">@csrf @method('delete')<button class="btn btn-sm btn-outline-danger">حذف</button></form></div>@empty<p class="text-muted">آدرسی ثبت نشده است.</p>@endforelse</div><div class="col-lg-5"><div class="ac-surface p-4"><h5>آدرس جدید</h5><form method="post" action="{{ route('account.addresses.store') }}">@csrf<div class="row g-2">@foreach(['title'=>'عنوان','full_name'=>'نام گیرنده','mobile'=>'موبایل','province'=>'استان','city'=>'شهر','postal_code'=>'کدپستی'] as $f=>$l)<div class="col-md-6"><label>{{ $l }}</label><input class="form-control" name="{{ $f }}" @if(in_array($f,['full_name','mobile','province','city'])) required @endif></div>@endforeach<div class="col-12"><label>نشانی</label><textarea class="form-control" name="address" required></textarea></div><div class="col-12"><label><input type="checkbox" name="is_default" value="1"> آدرس پیش‌فرض</label></div><div class="col-12"><button class="btn btn-primary w-100">ذخیره آدرس</button></div></div></form></div></div></div></div>@endsection
