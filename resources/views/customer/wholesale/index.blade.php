@extends('layouts.storefront') @section('title','خرید عمده | اتوکار') @section('content')<div class="container py-5"><h1>فروش عمده و همکاری</h1>@if(!$account || $account->status!=='approved')<div class="ac-surface p-4 mb-4"><h4>درخواست همکاری عمده</h4>@if($account)<div class="alert alert-info">وضعیت درخواست فعلی: {{ $account->status }}</div>@endif<form method="post" action="{{ route('account.wholesale.apply') }}">@csrf<div class="row g-2"><div class="col-md-6"><input class="form-control" name="company_name" value="{{ auth()->user()->legal_name }}" required placeholder="نام مجموعه"></div><div class="col-md-6"><input class="form-control" name="tax_id" value="{{ auth()->user()->national_id }}" placeholder="شناسه ملی / مالیاتی"></div></div><button class="btn btn-primary mt-3">ثبت درخواست</button></form></div>@else<div class="alert alert-success">حساب عمده شما فعال است. تخفیف پایه: {{ $account->discount_percent }}%</div><div class="ac-surface p-4 mb-4"><h4>ساخت استعلام / پیش‌فاکتور</h4><form method="post" action="{{ route('account.wholesale.quote') }}">@csrf<select class="form-select mb-2" name="items[0][product_slug]">@foreach($products as $p)<option value="{{ $p->slug }}">{{ $p->name }} — {{ $p->sku }}</option>@endforeach</select><input class="form-control mb-2" type="number" name="items[0][quantity]" min="1" value="1"><textarea class="form-control mb-2" name="note" placeholder="توضیح"></textarea><button class="btn btn-dark">ایجاد پیش‌فاکتور</button></form></div>@endif<div class="ac-surface p-4"><h4>پیش‌فاکتورها</h4>@forelse($quotes as $q)<div class="d-flex justify-content-between border-bottom py-3"><div><b>{{ $q->number }}</b><br><small>{{ $q->created_at }} — اعتبار تا {{ $q->expires_at }}</small></div><div class="text-end"><b>{{ number_format($q->total) }} ریال</b><br><a href="{{ route('account.wholesale.proforma',$q->number) }}" target="_blank">PDF پیش‌فاکتور</a></div></div>@empty<p>هنوز پیش‌فاکتوری ندارید.</p>@endforelse</div></div>@endsection
