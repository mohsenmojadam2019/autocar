@extends('layouts.storefront') @section('title','اعلان‌ها | اتوکار') @section('content')<div class="container py-5"><h1 class="h3 mb-4">اعلان‌ها</h1><div class="row g-4"><div class="col-lg-8"><div class="ac-surface p-4">@forelse($notifications as $notification)@php($data=json_decode($notification->data,true)?:[])<div class="border-bottom py-3"><b>{{ $data['title'] ?? 'اعلان اتوکار' }}</b><div>{{ $data['message'] ?? '' }}</div><small class="text-muted">{{ $notification->created_at }}</small></div>@empty<p class="text-muted">اعلانی ندارید.</p>@endforelse{{ $notifications->links() }}</div></div><div class="col-lg-4"><div class="ac-surface p-4"><h5>تنظیم کانال‌ها</h5><form method="post" action="{{ route('account.notifications.preferences') }}">@csrf @method('put')<label>رویداد</label><select class="form-select mb-3" name="event"><option value="order_status">وضعیت سفارش</option><option value="price_drop">کاهش قیمت</option><option value="back_in_stock">بازگشت موجودی</option></select>@foreach(['sms'=>'پیامک','email'=>'ایمیل','database'=>'داخل پنل','marketing'=>'تبلیغاتی'] as $f=>$l)<label class="form-check mb-2"><input class="form-check-input" type="checkbox" name="{{ $f }}" value="1"> {{ $l }}</label>@endforeach<button class="btn btn-primary w-100 mt-3">ذخیره</button></form></div></div></div></div>@endsection
