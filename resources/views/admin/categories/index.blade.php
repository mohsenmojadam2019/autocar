@extends('layouts.admin') @section('title','دسته‌بندی‌ها | اتوکار') @section('page_title','دسته‌بندی‌ها')
@section('content')<div class="admin-heading"><div><h1>درخت دسته‌بندی</h1><p>ساختار نامحدود؛ همه انتخاب‌ها و عملیات عمومی با Slug انجام می‌شوند.</p></div></div><div class="admin-two-col wide-first"><section class="admin-card"><h4>ساختار فعلی</h4>@php($render=function($items) use (&$render){ echo '<ul class="category-tree">'; foreach($items as $item){ echo '<li><span><i class="bi bi-folder2"></i> '.e($item->name).' <small class="text-muted">'.e($item->slug).'</small></span>'; if($item->children->isNotEmpty())$render($item->children); echo '</li>'; } echo '</ul>'; }) @php($render($tree))</section><aside class="admin-card"><h4>دسته جدید</h4><form method="post" action="{{ route('admin.categories.store') }}">@csrf<label>نام</label><input class="form-control mb-2" name="name" required><label>Slug</label><input class="form-control mb-2" name="slug"><label>دسته مادر</label><select class="form-select mb-2" name="parent_slug"><option value="">ریشه</option>@foreach($categories as $category)<option value="{{ $category->slug }}">{{ str_repeat('— ',$category->depth) }}{{ $category->name }} ({{ $category->slug }})</option>@endforeach</select><label>موقعیت</label><input class="form-control mb-2" name="position" type="number" value="0"><label class="form-check mb-3"><input class="form-check-input" type="checkbox" name="is_active" value="1" checked> فعال</label><button class="btn btn-primary w-100">ایجاد دسته</button></form></aside></div>@endsection
