@extends('layouts.admin') @section('title','کمپین‌ها | اتوکار') @section('page_title','بازاریابی و کمپین') @section('content')<div class="row g-4"><div class="col-lg-8"><div class="admin-card"><h4>کمپین‌های پیامکی</h4><div class="table-responsive"><table class="table"><thead><tr><th>نام</th><th>سگمنت</th><th>وضعیت</th><th>ارسال</th><th>خطا</th><th>عملیات</th></tr></thead><tbody>@foreach($campaigns as $c)<tr><td>{{ $c->name }}</td><td>{{ $c->segment_name }}</td><td>{{ $c->status }}</td><td>{{ $c->sent_count }}</td><td>{{ $c->failed_count }}</td><td class="d-flex gap-1"><form method="post" action="{{ route('admin.marketing.campaigns.recipients',$c->id) }}">@csrf<button class="btn btn-sm btn-outline-primary">ساخت گیرنده</button></form><form method="post" action="{{ route('admin.marketing.campaigns.stop',$c->id) }}">@csrf<button class="btn btn-sm btn-outline-danger">توقف</button></form></td></tr>@endforeach</tbody></table></div>{{ $campaigns->links() }}</div></div><div class="col-lg-4"><div class="admin-card mb-4"><h4>کمپین جدید</h4><form method="post" action="{{ route('admin.marketing.campaigns.store') }}">@csrf<input class="form-control mb-2" name="name" required placeholder="نام"><select class="form-select mb-2" name="segment_id"><option value="">همه رضایت‌داده‌ها</option>@foreach($segments as $s)<option value="{{ $s->id }}">{{ $s->name }}</option>@endforeach</select><textarea class="form-control mb-2" name="message" required placeholder="متن"></textarea><input class="form-control mb-2" type="datetime-local" name="scheduled_at"><input class="form-control mb-2" type="number" name="rate_per_minute" value="60"><button class="btn btn-primary w-100">ایجاد</button></form></div><div class="admin-card"><h4>سگمنت جدید</h4><form method="post" action="{{ route('admin.marketing.segments.store') }}">@csrf<input class="form-control mb-2" name="name" required placeholder="نام"><input class="form-control mb-2" name="customer_group" placeholder="retail / wholesale"><input class="form-control mb-2" type="number" name="min_lifetime_value" placeholder="حداقل LTV"><button class="btn btn-outline-primary w-100">ثبت سگمنت</button></form></div></div></div>@endsection
