@extends('layouts.admin') @section('title','کانبان سفارش | اتوکار') @section('page_title','کانبان سفارش') @section('content')<div class="d-flex gap-3 overflow-auto pb-3">@foreach(\App\Domain\Order\Enums\OrderStatus::cases() as $status)<section class="admin-card flex-shrink-0" style="width:300px"><h5>{{ $status->value }} <span class="badge text-bg-light">{{ ($orders[$status->value]??collect())->count() }}</span></h5>@forelse($orders[$status->value]??[] as $order)<a class="d-block border rounded p-2 mb-2 text-decoration-none" href="{{ route('admin.orders.show',$order) }}"><b>{{ $order->number }}</b><br><small>{{ $order->user?->name ?: 'مهمان/تلفنی' }}</small><br>{{ number_format($order->grand_total) }} ریال</a>@empty<small class="text-muted">سفارشی نیست.</small>@endforelse</section>@endforeach</div>@endsection