@extends('layouts.admin') @section('title','خودرو و سازگاری | اتوکار') @section('page_title','بانک خودرو و Fitment') @section('content')<div class="row g-4"><div class="col-lg-7"><div class="admin-card mb-4"><h4>بانک خودرو</h4>@foreach($makes as $make)<details class="border-bottom py-2"><summary><b>{{ $make->name }}</b> <code>{{ $make->slug }}</code></summary><div class="ps-3">@foreach($make->models as $model)<div class="py-2">{{ $model->name }}@foreach($model->generations as $generation)<div class="text-muted ps-3">{{ $generation->name }} ({{ $generation->from_year }}–{{ $generation->to_year }}) — {{ $generation->trims->count() }} تیپ</div>@endforeach</div>@endforeach</div></details>@endforeach</div><div class="admin-card"><h4>قواعد سازگاری اخیر</h4><div class="table-responsive"><table class="table"><thead><tr><th>محصول</th><th>خودرو</th><th>سال</th><th>نتیجه</th><th></th></tr></thead><tbody>@foreach($fitments as $f)<tr><td>{{ $f->product?->name }}<br><code>{{ $f->product?->slug }}</code></td><td>{{ $f->make?->name }} {{ $f->model?->name }} {{ $f->trim?->name }}</td><td>{{ $f->from_year }}-{{ $f->to_year }}</td><td>{{ $f->status->value }}</td><td><form method="post" action="{{ route('admin.vehicles.fitments.destroy',$f) }}">@csrf @method('delete')<button class="btn btn-sm btn-outline-danger">حذف</button></form></td></tr>@endforeach</tbody></table></div></div></div><div class="col-lg-5"><div class="admin-card mb-4"><h4>برند خودرو</h4><form class="row g-2" method="post" action="{{ route('admin.vehicles.makes.store') }}">@csrf<div class="col-6"><input class="form-control" name="name" required placeholder="نام"></div><div class="col-6"><input class="form-control" name="name_en" placeholder="English"></div><div class="col-8"><input class="form-control" name="slug" placeholder="slug"></div><div class="col-4"><button class="btn btn-primary w-100">ثبت</button></div></form></div><div class="admin-card"><h4>قانون سازگاری محصول</h4><form method="post" action="{{ route('admin.vehicles.fitments.store') }}">@csrf<label>محصول (Slug)</label><select class="form-select mb-2" name="product_slug" required>@foreach($products as $p)<option value="{{ $p->slug }}">{{ $p->name }} — {{ $p->slug }}</option>@endforeach</select><label>برند خودرو</label><select class="form-select mb-2" name="vehicle_make_id"><option value="">همه</option>@foreach($makes as $make)<option value="{{ $make->id }}">{{ $make->name }}</option>@endforeach</select><div class="row g-2"><div class="col-6"><input class="form-control" type="number" name="from_year" placeholder="از سال"></div><div class="col-6"><input class="form-control" type="number" name="to_year" placeholder="تا سال"></div><div class="col-6"><select class="form-select" name="status"><option value="compatible">سازگار</option><option value="conditional">مشروط</option><option value="incompatible">ناسازگار</option></select></div><div class="col-6"><input class="form-control" type="number" name="confidence" value="100" min="0" max="100"></div><div class="col-12"><label><input type="checkbox" name="is_exclusion" value="1"> قانون استثنا/عدم سازگاری</label></div><div class="col-12"><textarea class="form-control" name="notes" placeholder="توضیح"></textarea></div><div class="col-12"><button class="btn btn-primary w-100">ثبت Fitment</button></div></div></form></div></div></div>@endsection
