@extends('layouts.admin') @section('title','عملیات و سلامت | اتوکار') @section('page_title','Operations') @section('content')<div class="row g-4 mb-4"><div class="col-md-3"><div class="admin-card"><small>Queue</small><h2>{{ number_format($queuedJobs) }}</h2></div></div><div class="col-md-3"><div class="admin-card"><small>Failed Jobs</small><h2>{{ number_format($failedJobs) }}</h2></div></div><div class="col-md-6"><div class="admin-card d-flex gap-2"><form method="post" action="{{ route('admin.operations.backup') }}">@csrf<button class="btn btn-primary">ایجاد Backup</button></form><form method="post" action="{{ route('admin.operations.health.run') }}">@csrf<button class="btn btn-outline-primary">Health Check</button></form></div></div></div><div class="row g-4"><div class="col-lg-7"><div class="admin-card table-responsive"><h4>Provider Health</h4><table class="table"><thead><tr><th>نوع</th><th>Provider</th><th>وضعیت</th><th>Latency</th><th>تاریخ</th></tr></thead><tbody>@foreach($health as $h)<tr><td>{{ $h->provider_type }}</td><td>{{ $h->provider_name }}</td><td>{{ $h->status }}</td><td>{{ $h->latency_ms }}ms</td><td>{{ $h->checked_at }}</td></tr>@endforeach</tbody></table></div></div><div class="col-lg-5"><div class="admin-card"><h4>Backup</h4>@foreach($backups as $b)<div class="border-bottom py-2"><b>{{ $b->status }}</b> — {{ number_format($b->size_bytes ?? 0) }} bytes<br><small>{{ $b->checksum }}<br>{{ $b->started_at }}</small></div>@endforeach</div></div></div>@endsection
