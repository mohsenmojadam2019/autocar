@extends('layouts.admin') @section('title',$title.' | اتوکار') @section('page_title',$title) @section('content')<div class="admin-heading"><div><h1>{{ $title }}</h1><p>{{ $description }}</p></div></div><div class="admin-card"><div class="table-responsive"><table class="table align-middle"><thead><tr>@foreach($columns as $label)<th>{{ $label }}</th>@endforeach</tr></thead><tbody>@forelse($rows as $row)<tr>@foreach($columns as $field=>$label)<td>@if(str_contains($field,'amount')||str_contains($field,'total')){{ number_format((int)($row->{$field}??0)) }}@else{{ $row->{$field} ?? '—' }}@endif</td>@endforeach</tr>@empty<tr><td colspan="{{ count($columns) }}" class="text-center text-muted py-5">داده‌ای نیست.</td></tr>@endforelse</tbody></table></div>{{ $rows->links() }}</div>@endsection
