@extends('layouts.admin') @section('title','نسخه‌های محتوا | اتوکار') @section('page_title','Revision History') @section('content')<div class="admin-card"><h3>{{ $model->title }}</h3><table class="table"><thead><tr><th>نسخه</th><th>تاریخ</th><th>یادداشت</th><th></th></tr></thead><tbody>@foreach($revisions as $r)<tr><td>#{{ $r->id }}</td><td>{{ $r->created_at }}</td><td>{{ $r->note }}</td><td><form method="post" action="{{ route('admin.editorial.revisions.restore',['type'=>$type,'slug'=>$model->slug,'revision'=>$r->id]) }}">@csrf<button class="btn btn-sm btn-outline-primary">بازیابی</button></form></td></tr>@endforeach</tbody></table></div>@endsection
