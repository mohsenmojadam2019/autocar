@extends('layouts.admin') @section('title','مدیریت محتوا | اتوکار') @section('page_title','صفحات، بلاگ و FAQ') @section('content')<div class="row g-4"><div class="col-xl-8"><div class="admin-card table-responsive"><h4>صفحات</h4><table class="table"><thead><tr><th>عنوان</th><th>Slug</th><th>وضعیت</th><th>انتشار</th><th></th></tr></thead><tbody>@foreach($pages as $p)<tr><td>{{ $p->title }}</td><td>{{ $p->slug }}</td><td>{{ $p->status }}</td><td>{{ $p->published_at }}</td><td><a href="{{ route('admin.editorial.revisions',['type'=>'page','slug'=>$p->slug]) }}">نسخه‌ها</a></td></tr>@endforeach</tbody></table>{{ $pages->links() }}</div><div class="admin-card table-responsive mt-3"><h4>مقالات</h4><table class="table"><thead><tr><th>عنوان</th><th>Slug</th><th>وضعیت</th><th>انتشار</th><th></th></tr></thead><tbody>@foreach($posts as $p)<tr><td>{{ $p->title }}</td><td>{{ $p->slug }}</td><td>{{ $p->status }}</td><td>{{ $p->published_at }}</td><td><a href="{{ route('admin.editorial.revisions',['type'=>'post','slug'=>$p->slug]) }}">نسخه‌ها</a></td></tr>@endforeach</tbody></table>{{ $posts->links() }}</div></div><div class="col-xl-4"><div class="admin-card mb-3"><h4>صفحه جدید</h4><form method="post" action="{{ route('admin.editorial.pages.store') }}">@csrf<input class="form-control mb-2" name="title" placeholder="عنوان" required><input class="form-control mb-2" name="slug" placeholder="slug"><textarea class="form-control mb-2" name="body" rows="5" placeholder="محتوا؛ HTML امن پشتیبانی می‌شود"></textarea><select class="form-select mb-2" name="status"><option value="draft">پیش‌نویس</option><option value="published">منتشر</option></select><input class="form-control mb-2" name="published_at" data-jalali-date placeholder="تاریخ انتشار جلالی"><button class="btn btn-primary w-100">ایجاد صفحه</button></form></div><div class="admin-card"><h4>مقاله جدید</h4><form method="post" action="{{ route('admin.editorial.posts.store') }}">@csrf<input class="form-control mb-2" name="title" required placeholder="عنوان"><input class="form-control mb-2" name="slug" placeholder="slug"><textarea class="form-control mb-2" name="excerpt" placeholder="خلاصه"></textarea><textarea class="form-control mb-2" name="body" rows="6" required placeholder="متن"></textarea><select class="form-select mb-2" name="status"><option value="draft">پیش‌نویس</option><option value="published">منتشر</option></select><input class="form-control mb-2" name="published_at" data-jalali-date placeholder="تاریخ انتشار جلالی"><button class="btn btn-dark w-100">ایجاد مقاله</button></form></div></div></div>@endsection
