@extends('layouts.admin') @section('title','نظرات و پرسش‌ها | اتوکار') @section('page_title','Moderation') @section('content')<div class="admin-card table-responsive mb-4"><h4>نظرات</h4><table class="table"><thead><tr><th>محصول</th><th>کاربر</th><th>امتیاز</th><th>متن</th><th>وضعیت</th><th>عملیات</th></tr></thead><tbody>@foreach($reviews as $r)<tr><td>{{ $r->product_name }}</td><td>{{ $r->user_name }}</td><td>{{ $r->rating }}</td><td>{{ $r->body }}</td><td>{{ $r->status }}</td><td><form method="post" action="{{ route('admin.moderation.reviews',$r->id) }}">@csrf @method('patch')<select class="form-select form-select-sm mb-1" name="status"><option value="approved">تأیید</option><option value="rejected">رد</option></select><input class="form-control form-control-sm mb-1" name="admin_reply" placeholder="پاسخ فروشگاه"><button class="btn btn-sm btn-primary">ثبت</button></form></td></tr>@endforeach</tbody></table>{{ $reviews->links() }}</div><div class="row g-4"><div class="col-lg-7"><div class="admin-card"><h4>پرسش محصول</h4>@foreach($questions as $q)<div class="border-bottom py-3"><b>{{ $q->product_name }}</b><p>{{ $q->question }}</p><form method="post" action="{{ route('admin.moderation.questions',$q->id) }}">@csrf @method('patch')<textarea class="form-control mb-2" name="answer" placeholder="پاسخ"></textarea><div class="d-flex gap-2"><button class="btn btn-success" name="status" value="approved">تأیید و پاسخ</button><button class="btn btn-outline-danger" name="status" value="rejected">رد</button></div></form></div>@endforeach{{ $questions->links() }}</div></div><div class="col-lg-5"><div class="admin-card"><h4>گزارش تخلف</h4>@foreach($reports as $r)<div class="border-bottom py-3"><p>{{ $r->reason }} — {{ $r->review_body }}</p><form method="post" action="{{ route('admin.moderation.reports',$r->id) }}">@csrf @method('patch')<input class="form-control mb-2" name="moderator_note" placeholder="یادداشت"><button class="btn btn-sm btn-primary" name="status" value="resolved">رسیدگی شد</button> <button class="btn btn-sm btn-light" name="status" value="dismissed">رد گزارش</button></form></div>@endforeach</div></div></div>@endsection
