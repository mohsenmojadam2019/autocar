@extends('layouts.admin') @section('title','پرداخت دستی | اتوکار') @section('page_title','رسیدهای کارت‌به‌کارت') @section('content')<div class="admin-card table-responsive"><table class="table align-middle"><thead><tr><th>سفارش</th><th>مبلغ</th><th>پیگیری</th><th>۴ رقم</th><th>وضعیت</th><th>عملیات</th></tr></thead><tbody>@foreach($proofs as $p)<tr><td>{{ $p->order_number }}</td><td>{{ number_format($p->amount) }}</td><td>{{ $p->reference_code }}</td><td>{{ $p->card_last4 }}</td><td>{{ $p->status }}</td><td>@if($p->status==='pending')<div class="d-flex gap-1"><form method="post" action="{{ route('admin.payments.manual.approve',$p->id) }}">@csrf<button class="btn btn-sm btn-success">تأیید</button></form><form method="post" action="{{ route('admin.payments.manual.reject',$p->id) }}">@csrf<button class="btn btn-sm btn-danger">رد</button></form></div>@endif</td></tr>@endforeach</tbody></table>{{ $proofs->links() }}</div>@endsection
