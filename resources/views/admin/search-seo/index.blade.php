@extends('layouts.admin') @section('title','جست‌وجو و SEO | اتوکار') @section('page_title','جست‌وجو و SEO') @section('content')<div class="row g-4"><div class="col-xl-7"><div class="admin-card"><h4>تحلیل جست‌وجو</h4><table class="table"><thead><tr><th>عبارت</th><th>دفعات</th><th>میانگین نتیجه</th></tr></thead><tbody>@foreach($searches as $s)<tr><td>{{ $s->term }}</td><td>{{ $s->searches }}</td><td>{{ number_format($s->avg_results,1) }}</td></tr>@endforeach</tbody></table></div><div class="admin-card mt-3"><h4>Redirectها</h4>@foreach($redirects as $r)<div class="d-flex justify-content-between border-bottom py-2"><div><code>{{ $r->from_path }}</code> → {{ $r->to_url }}<br><small>{{ $r->status_code }} / {{ $r->hits }} hit / {{ $r->is_active?'فعال':'غیرفعال' }}</small></div><form method="post" action="{{ route('admin.search-seo.redirects.toggle',$r->id) }}">@csrf @method('patch')<button class="btn btn-sm btn-outline-secondary">تغییر وضعیت</button></form></div>@endforeach</div></div><div class="col-xl-5"><div class="admin-card mb-3"><h4>مترادف جدید</h4><form method="post" action="{{ route('admin.search-seo.synonyms.store') }}">@csrf<div class="row g-2"><div class="col-5"><input class="form-control" name="term" required placeholder="روغن موتور"></div><div class="col-5"><input class="form-control" name="synonym" required placeholder="اویل"></div><div class="col-2"><input class="form-control" name="weight" type="number" value="100"></div></div><button class="btn btn-primary mt-2 w-100">ذخیره</button></form>@foreach($synonyms as $s)<div class="d-flex justify-content-between py-2 border-bottom"><span>{{ $s->term }} ↔ {{ $s->synonym }}</span><form method="post" action="{{ route('admin.search-seo.synonyms.destroy',$s->id) }}">@csrf @method('delete')<button class="btn btn-sm btn-link text-danger">حذف</button></form></div>@endforeach</div><div class="admin-card"><h4>SEO Redirect</h4><form method="post" action="{{ route('admin.search-seo.redirects.store') }}">@csrf<input class="form-control mb-2" name="from_path" required placeholder="/old-page"><input class="form-control mb-2" name="to_url" required placeholder="/new-page یا URL کامل"><select class="form-select mb-2" name="status_code"><option>301</option><option>302</option><option>307</option><option>308</option></select><button class="btn btn-dark w-100">ثبت Redirect</button></form></div></div></div>@endsection
