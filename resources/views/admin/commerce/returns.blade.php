@extends('layouts.admin') @section('title','مرجوعی | اتوکار') @section('page_title','RMA، بازپرداخت و بازگشت موجودی') @section('content')<div class="admin-card table-responsive"><table class="table align-middle"><thead><tr><th>RMA</th><th>سفارش</th><th>مشتری</th><th>وضعیت</th><th>درخواست</th><th>تأیید</th><th>تاریخ</th><th>عملیات</th></tr></thead><tbody>@foreach($returns as $r)<tr><td>{{ $r->number }}</td><td>{{ $r->order_number }}</td><td>{{ $r->customer_name }}</td><td>{{ $r->status }}</td><td>{{ number_format($r->requested_refund) }}</td><td>{{ number_format($r->approved_refund) }}</td><td>{{ $r->created_at }}</td><td>@if(in_array($r->status,['requested','reviewing']))<form class="d-flex gap-1 mb-1" method="post" action="{{ route('admin.returns.approve',$r->id) }}">@csrf<input class="form-control form-control-sm" type="number" name="approved_refund" min="0" value="0" placeholder="بازپرداخت"><input class="form-control form-control-sm" name="admin_note" placeholder="یادداشت"><button class="btn btn-sm btn-success">تأیید</button></form><form class="d-flex gap-1" method="post" action="{{ route('admin.returns.reject',$r->id) }}">@csrf<input class="form-control form-control-sm" name="admin_note" required placeholder="دلیل رد"><button class="btn btn-sm btn-outline-danger">رد</button></form>@endif</td></tr>@endforeach</tbody></table>{{ $returns->links() }}</div>@endsection
