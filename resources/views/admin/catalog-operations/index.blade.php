@extends('layouts.admin') @section('title','ورود و خروج کاتالوگ | اتوکار') @section('page_title','Import / Export') @section('content')<div class="row g-4"><div class="col-lg-4"><div class="admin-card mb-4"><h4>Import CSV</h4><p class="text-muted">شناسه خارجی محصول، برند و دسته در فایل فقط Slug است.</p><form method="post" enctype="multipart/form-data" action="{{ route('admin.catalog-operations.import') }}">@csrf<input class="form-control mb-3" type="file" name="file" accept=".csv,.txt" required><button class="btn btn-primary w-100">پردازش فایل</button></form></div><div class="admin-card"><h4>Export</h4><a class="btn btn-outline-primary w-100" href="{{ route('admin.catalog-operations.export') }}">دانلود CSV کامل</a></div></div><div class="col-lg-8"><div class="admin-card"><h4>تاریخچه Import</h4><div class="table-responsive"><table class="table"><thead><tr><th>#</th><th>فایل</th><th>وضعیت</th><th>موفق</th><th>خطا</th><th></th></tr></thead><tbody>@foreach($imports as $import)<tr><td>{{ $import->id }}</td><td>{{ $import->file_name }}</td><td>{{ $import->status }}</td><td>{{ $import->processed_rows }}</td><td>{{ $import->failed_rows }}</td><td>@if($import->failed_rows)<a href="{{ route('admin.catalog-operations.errors',$import->id) }}">خطاها</a>@endif</td></tr>@endforeach</tbody></table></div>{{ $imports->links() }}</div></div></div>@endsection
