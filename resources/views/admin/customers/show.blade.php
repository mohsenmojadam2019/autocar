@extends('layouts.admin') @section('title',$customer->name.' | CRM') @section('page_title','نمای ۳۶۰ مشتری') @section('content')<div class="row g-4"><div class="col-lg-8"><div class="admin-card mb-4"><h3>{{ $customer->name }}</h3><p>{{ $customer->mobile }} — {{ $customer->email }}</p><div class="row"><div class="col">LTV: <b>{{ number_format($profile->lifetime_value??0) }}</b></div><div class="col">سفارش: <b>{{ $profile->orders_count??0 }}</b></div><div class="col">گروه: <b>{{ $profile->customer_group??'retail' }}</b></div></div></div><div class="admin-card"><h4>آخرین سفارش‌ها</h4>@foreach($orders as $order)<div class="d-flex justify-content-between border-bottom py-2"><span>{{ $order->number }} — {{ $order->status }}</span><b>{{ number_format($order->grand_total) }}</b></div>@endforeach</div></div><div class="col-lg-4"><div class="admin-card mb-4"><h4>خودروهای گاراژ</h4>@foreach($customer->vehicles as $v)<div class="border-bottom py-2">{{ $v->nickname ?: $v->trim?->generation?->model?->make?->name }} {{ $v->trim?->generation?->model?->name }} {{ $v->trim?->year }}</div>@endforeach</div><div class="admin-card"><h4>یادداشت داخلی</h4><form method="post" action="{{ route('admin.customers.note',$customer) }}">@csrf<textarea class="form-control mb-2" name="note" required></textarea><label><input type="checkbox" name="is_pinned" value="1"> سنجاق شود</label><button class="btn btn-primary w-100 mt-2">ثبت</button></form>@foreach($notes as $note)<div class="border-top mt-3 pt-2">{{ $note->note }}</div>@endforeach</div></div></div>@endsection
