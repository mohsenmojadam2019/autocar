@extends('layouts.admin') @section('title','برندها | اتوکار') @section('page_title','برند قطعات') @section('content')<div class="row g-4"><div class="col-lg-8"><div class="admin-card"><div class="table-responsive"><table class="table"><thead><tr><th>نام</th><th>Slug</th><th>کشور</th><th>محصول</th><th>فعال</th><th></th></tr></thead><tbody>@foreach($brands as $brand)<tr><td>{{ $brand->name }}</td><td><code>{{ $brand->slug }}</code></td><td>{{ $brand->country_code }}</td><td>{{ $brand->products_count }}</td><td>{{ $brand->is_active?'بله':'خیر' }}</td><td><form method="post" action="{{ route('admin.brands.destroy',$brand) }}">@csrf @method('delete')<button class="btn btn-sm btn-outline-danger">حذف</button></form></td></tr>@endforeach</tbody></table></div>{{ $brands->links() }}</div></div><div class="col-lg-4"><div class="admin-card"><h4>برند جدید</h4><form method="post" action="{{ route('admin.brands.store') }}">@csrf<input class="form-control mb-2" name="name" required placeholder="نام فارسی"><input class="form-control mb-2" name="name_en" placeholder="English name"><input class="form-control mb-2" name="slug" placeholder="slug"><input class="form-control mb-2" name="country_code" maxlength="2" placeholder="IR / KR / DE"><textarea class="form-control mb-2" name="description" placeholder="توضیح"></textarea><label class="form-check mb-3"><input class="form-check-input" type="checkbox" name="is_active" value="1" checked> فعال</label><button class="btn btn-primary w-100">ایجاد</button></form></div></div></div>@endsection
