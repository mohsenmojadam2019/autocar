@extends('layouts.admin') @section('title','امنیت و دسترسی | اتوکار') @section('page_title','Role / Permission / Audit') @section('content')<div class="row g-4"><div class="col-lg-8"><div class="admin-card mb-4"><h4>کاربران و نقش‌ها</h4><div class="table-responsive"><table class="table"><thead><tr><th>کاربر</th><th>نقش فعلی</th><th>تخصیص</th><th>2FA</th></tr></thead><tbody>@foreach($users as $u)<tr><td>{{ $u->name }}<br><small>{{ $u->mobile }}</small></td><td>{{ $u->roles->pluck('name')->join('، ') }}</td><td><form class="d-flex gap-1" method="post" action="{{ route('admin.security.users.roles',$u) }}">@csrf @method('put')<select class="form-select form-select-sm" multiple name="roles[]">@foreach($roles as $r)<option value="{{ $r->id }}" @selected($u->roles->contains($r))>{{ $r->name }}</option>@endforeach</select><button class="btn btn-sm btn-primary">ذخیره</button></form></td><td><form method="post" action="{{ route('admin.security.2fa.reset',$u) }}">@csrf<button class="btn btn-sm btn-outline-warning">Reset</button></form></td></tr>@endforeach</tbody></table></div>{{ $users->links() }}</div><div class="admin-card"><h4>Audit اخیر</h4><div class="table-responsive"><table class="table"><thead><tr><th>رویداد</th><th>کاربر</th><th>IP</th><th>زمان</th></tr></thead><tbody>@foreach($auditLogs as $log)<tr><td>{{ $log->event }}</td><td>{{ $log->user_id }}</td><td>{{ $log->ip_address }}</td><td>{{ $log->created_at }}</td></tr>@endforeach</tbody></table></div></div></div><div class="col-lg-4"><div class="admin-card mb-4"><h4>نقش جدید</h4><form method="post" action="{{ route('admin.security.roles.store') }}">@csrf<input class="form-control mb-2" name="name" required placeholder="نام نقش"><input class="form-control mb-2" name="slug" placeholder="slug"><div style="max-height:250px;overflow:auto">@foreach($permissions as $p)<label class="form-check"><input class="form-check-input" type="checkbox" name="permissions[]" value="{{ $p->id }}"> {{ $p->slug }}</label>@endforeach</div><button class="btn btn-primary w-100 mt-2">ایجاد نقش</button></form></div><div class="admin-card"><h4>قانون IP</h4><form method="post" action="{{ route('admin.security.ip-rules.store') }}">@csrf<input class="form-control mb-2" name="cidr" required placeholder="192.168.1.0/24"><select class="form-select mb-2" name="is_allowed"><option value="1">Allow</option><option value="0">Deny</option></select><input class="form-control mb-2" name="note" placeholder="توضیح"><button class="btn btn-outline-primary w-100">ثبت</button></form>@foreach($ipRules as $r)<div class="border-top mt-2 pt-2"><code>{{ $r->cidr }}</code> — {{ $r->is_allowed?'Allow':'Deny' }}</div>@endforeach</div></div></div>@endsection
