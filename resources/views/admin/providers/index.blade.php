@extends('layouts.admin') @section('title','Providerها | اتوکار') @section('page_title','درگاه و پیامک') @section('content')<div class="alert alert-warning">Secretهای ذخیره‌شده عمداً هیچ‌وقت از سرور به HTML برنمی‌گردند. فیلد Secret خالی یعنی مقدار موجود حفظ شود.</div><div class="row g-4"><div class="col-lg-6"><div class="admin-card"><h4>درگاه پرداخت</h4><form method="post" action="{{ route('admin.providers.payment') }}">@csrf<select class="form-select mb-2" name="provider">@foreach($payments as $p)<option value="{{ $p['name'] }}">{{ $p['name'] }} {{ $p['configured']?'✓':'' }}</option>@endforeach</select><input class="form-control mb-2" type="password" autocomplete="new-password" name="api_key" placeholder="API Key جدید"><input class="form-control mb-2" type="password" autocomplete="new-password" name="merchant_id" placeholder="Merchant ID (برای زرین‌پال)"><label class="form-check mb-2"><input class="form-check-input" type="checkbox" name="sandbox" value="1"> Sandbox</label><input class="form-control mb-2" name="request_url" placeholder="Request URL"><input class="form-control mb-2" name="verify_url" placeholder="Verify URL"><input class="form-control mb-2" name="refund_url" placeholder="Refund URL"><input class="form-control mb-2" name="redirect_url" placeholder="Redirect URL با {authority}"><button class="btn btn-primary w-100">ذخیره درگاه</button></form></div></div><div class="col-lg-6"><div class="admin-card"><h4>Provider پیامک</h4><form method="post" action="{{ route('admin.providers.sms') }}">@csrf<select class="form-select mb-2" name="provider">@foreach($sms as $p)<option value="{{ $p['name'] }}">{{ $p['name'] }} {{ $p['configured']?'✓':'' }}</option>@endforeach</select><input class="form-control mb-2" type="password" autocomplete="new-password" name="api_key" placeholder="API Key جدید"><input class="form-control mb-2" name="sender" placeholder="Sender"><input class="form-control mb-2" name="send_url" placeholder="Send URL"><input class="form-control mb-2" name="pattern_url" placeholder="Pattern URL"><input class="form-control mb-2" name="status_url" placeholder="Status URL"><button class="btn btn-dark w-100">ذخیره پیامک</button></form></div></div></div><form class="mt-4" method="post" action="{{ route('admin.providers.health') }}">@csrf<button class="btn btn-outline-primary">اجرای Health Check بدون تراکنش مالی</button></form>@endsection
